# Copy to .env.local and fill in. Never commit the real file.

# Database (TiDB Cloud Serverless — reachable from anywhere, TLS required)
DB_HOST=            # e.g. gateway01.us-east-1.prod.aws.tidbcloud.com
DB_PORT=4000
DB_NAME=utilities
DB_USER=            # e.g. xxxxxxxxxxxx.root
DB_PASS=
DB_USE_SSL=true
DB_SSL_CA_PATH=     # optional; TiDB certs verify against public CAs without it

# One-time migration source (legacy UVM webdb; only needed for scripts/migrate-to-tidb.ts)
SRC_DB_HOST=webdb.uvm.edu
SRC_DB_NAME=
SRC_DB_USER=
SRC_DB_PASS=

# Email (iCloud SMTP)
EMAIL_PASS=
SMTP_HOST=smtp.mail.me.com
SMTP_PORT=587
APP_EMAIL_FROM_ADDRESS=
APP_EMAIL_FROM_NAME=77 N Union Utilities
APP_CONFIRMATION_EMAIL_TO=

# App
APP_BASE_URL=https://utilities.aaronperkel.com
APP_BILLS_PER_PAGE=10

# Auth (interim passphrase gate; CAS was removed when the site left UVM hosting)
SESSION_SECRET=          # openssl rand -hex 32
SITE_PASSPHRASE=         # what the login page asks for; login always fails while unset
SITE_OWNER_UID=aperkel   # tblPeople.uid the passphrase session logs in as
APP_LOCAL_DEV_USER=      # set to a uid to bypass login locally; empty in production

# Bill PDF storage (Vercel Blob — token from the store's page under Vercel → Storage)
BLOB_READ_WRITE_TOKEN=
BILLS_DIR=          # only the scripts/migrate-pdfs-to-blob.ts source dir; defaults to ./bill-pdfs

# Public unpaid-bills API (leave empty to disable the endpoint)
API_KEY=
HMAC_KEY=

# Hourly reminder cron (app/api/cron/reminders); same value lives as a GitHub
# Actions repo secret and must be set in the Vercel project env.
CRON_SECRET=
